<h1><%= @person.name %></h1>

<ul>
  <li>
    <%= @person.company.name %> - <%= @person.departament.name %>
  </li>
  <li>
    Ramal: <%= @person.branch_line %>
  </li>
</ul>

<%= link_to 'Editar', edit_person_path(@person.id) %>